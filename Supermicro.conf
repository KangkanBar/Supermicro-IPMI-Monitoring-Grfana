#Global tags can be specified here in key="value" format.
[global_tags]
  # dc = "us-east-1" # will tag all metrics with dc=us-east-1
  # rack = "1a"
  ## Environment variables can be used as tags, and throughout the config file
  # user = "$USER"


# Configuration for telegraf agent
[agent]
  ## Default data collection interval for all inputs
  interval = "10s"
  ## Rounds collection interval to 'interval'
  ## ie, if interval="10s" then always collect on :00, :10, :20, etc.
  round_interval = true

  ## Telegraf will send metrics to outputs in batches of at most
  ## metric_batch_size metrics.
  ## This controls the size of writes that Telegraf sends to output plugins.
  metric_batch_size = 1000

  ## For failed writes, telegraf will cache metric_buffer_limit metrics for each
  ## output, and will flush this buffer on a successful write. Oldest metrics
  ## are dropped first when this buffer fills.
  ## This buffer only fills when writes fail to output plugin(s).
  metric_buffer_limit = 10000

  ## Collection jitter is used to jitter the collection by a random amount.
  ## Each plugin will sleep for a random time within jitter before collecting.
  ## This can be used to avoid many plugins querying things like sysfs at the
  ## same time, which can have a measurable effect on the username.
  collection_jitter = "0s"

  ## Default flushing interval for all outputs. Maximum flush_interval will be
  ## flush_interval + flush_jitter
  flush_interval = "10s"
  ## Jitter the flush interval by a random amount. This is primarily to avoid
  ## large write spikes for users running a large number of telegraf instances.
  ## ie, a jitter of 5s and interval 10s means flushes will happen every 10-15s
  flush_jitter = "0s"

  ## By default or when set to "0s", precision will be set to the same
  ## timestamp order as the collection interval, with the maximum being 1s.
  ##   ie, when interval = "10s", precision will be "1s"
  ##       when interval = "250ms", precision will be "1ms"
  ## Precision will NOT be used for service inputs. It is up to each individual
  ## service input to set the timestamp at the appropriate precision.
  ## Valid time units are "ns", "us" (or "µs"), "ms", "s".
  precision = "0s"

  ## Logging configuration:
  ## Run telegraf with debug log messages.
  debug = true
  ## Run telegraf in quiet mode (error log messages only).
  quiet = false
  ## Specify the log file name. The empty string means to log to stderr.
  #logtarget = "file"    
  logfile = ""

  ## Override default hostname, if empty use os.Hostname()
  hostname = ""
  ## If set to true, do no set the "host" tag in the telegraf agent.
  omit_hostname = false


###############################################################################
#                            OUTPUT PLUGINS                                   #
###############################################################################

# Configuration for sending metrics to InfluxDB
[[outputs.influxdb]]
  ## The full HTTP or UDP URL for your InfluxDB instance.
  ##
  ## Multiple URLs can be specified for a single cluster, only ONE of the
  ## urls will be written to each interval.
  # urls = ["unix:///var/run/influxdb.sock"]
  # urls = ["udp://127.0.0.1:8089"]
   urls = ["http://127.0.0.1:8086"]

  ## The target database for metrics; will be created as needed.
   database = "ipmisupermicrodb"

  ## If true, no CREATE DATABASE queries will be sent.  Set to true when using
  ## Telegraf with a user without permissions to create databases or when the
  ## database already exists.
  # skip_database_creation = false

  ## Name of existing retention policy to write to.  Empty string writes to
  ## the default retention policy.  Only takes effect when using HTTP.
  # retention_policy = ""

  ## Write consistency (clusters only), can be: "any", "one", "quorum", "all".
  ## Only takes effect when using HTTP.
  # write_consistency = "any"

  ## Timeout for HTTP messages.
  # timeout = "5s"

  ## HTTP Basic Auth
   username = "ipmiuser"
   password = "FGHRYDFHGCHVFFDGGHDGHD"
   namepass = ["hdd_status", "ipmi_sensor"]
  ## HTTP User-Agent
  # user_agent = "telegraf"

  ## UDP payload size is the maximum packet size to send.
  # udp_payload = 512

  ## Optional TLS Config for use on HTTP connections.
  # tls_ca = "/etc/telegraf/ca.pem"
  # tls_cert = "/etc/telegraf/cert.pem"
  # tls_key = "/etc/telegraf/key.pem"
  ## Use TLS but skip chain & host verification
  # insecure_skip_verify = false

  ## HTTP Proxy override, if unset values the standard proxy environment
  ## variables are consulted to determine which proxy, if any, should be used.
  # http_proxy = "http://corporate.proxy:3128"

  ## Additional HTTP headers
  # http_headers = {"X-Special-Header" = "Special-Value"}

  ## HTTP Content-Encoding for write request body, can be set to "gzip" to
  ## compress body or "identity" to apply no encoding.
  # content_encoding = "identity"

  ## When true, Telegraf will output unsigned integers as unsigned values,
  ## i.e.: "42u".  You will need a version of InfluxDB supporting unsigned
  ## integer values.  Enabling this option will result in field type errors if
  ## existing data has been written.
  # influx_uint_support = false
 
 # # Read metrics from the bare metal servers via IPMI
 [[inputs.ipmi_sensor]]
#   ## optionally specify the path to the ipmitool executable
#   # path = "/usr/bin/ipmitool"
#   ##
#   ## optionally force session privilege level. Can be CALLBACK, USER, OPERATOR, ADMINISTRATOR
#   # privilege = "ADMINISTRATOR"
#   ##
#   ## optionally specify one or more servers via a url matching
#   ##  [username[:password]@][protocol[(address)]]
#   ##  e.g.
#   ##    root:passwd@lanplus(127.0.0.1)
#   ##
#   ## if no servers are specified, local machine sensor stats will be queried
#   ##
    servers = [
	"username:password@lanplus(192.168.0.175)",
	"username:password@lanplus(192.168.0.102)",
	"username:password@lanplus(192.168.0.195)",
	"username:password@lanplus(192.168.0.171)",
	"username:password@lanplus(192.168.0.96)",
	"username:password@lanplus(192.168.0.135)",
	"username:password@lanplus(192.168.0.188)",
	"username:password@lanplus(192.168.0.25)",
	"username:password@lanplus(192.168.0.101)",
	"username:password@lanplus(192.168.0.196)",
	"username:password@lanplus(192.168.0.197)",
	"username:password@lanplus(192.168.0.198)",
	"username:password@lanplus(192.168.0.200)",
	"username:password@lanplus(192.168.0.201)",
	"username:password@lanplus(192.168.0.202)",
	"username:password@lanplus(192.168.0.179)",
	"username:password@lanplus(192.168.0.180)",
	"username:password@lanplus(192.168.0.181)",
	"username:password@lanplus(192.168.0.183)",
	"username:password@lanplus(192.168.0.184)",
	"username:password@lanplus(192.168.0.186)",
	"username:password@lanplus(192.168.0.185)",
	"username:password@lanplus(192.168.0.106)",
	"username:password@lanplus(192.168.0.107)",
	"username:password@lanplus(192.168.0.108)",
	"username:password@lanplus(192.168.0.112)",
	"username:password@lanplus(192.168.0.97)",
	"username:password@lanplus(192.168.0.98)",
	"username:password@lanplus(192.168.0.100)",
	"username:password@lanplus(192.168.0.103)",
	"username:password@lanplus(192.168.0.130)",
	"username:password@lanplus(192.168.0.136)",
	"username:password@lanplus(192.168.0.137)",
	"username:password@lanplus(192.168.0.138)",
	"username:password@lanplus(192.168.0.139)",
	"username:password@lanplus(192.168.0.140)",
	"username:password@lanplus(192.168.0.141)",
	"username:password@lanplus(192.168.0.142)",
	"username:password@lanplus(192.168.0.81)",
	"username:password@lanplus(192.168.0.82)",
	"username:password@lanplus(192.168.0.83)",
	"username:password@lanplus(192.168.0.84)",
	"username:password@lanplus(192.168.0.88)",
	"username:password@lanplus(192.168.0.86)",
	"username:password@lanplus(192.168.0.87)",
	"username:password@lanplus(192.168.0.85)",
	"username:password@lanplus(192.168.0.113)",
	"username:password@lanplus(192.168.0.114)",
	"username:password@lanplus(192.168.0.115)",
	"username:password@lanplus(192.168.0.116)",
	"username:password@lanplus(192.168.0.117)",
	"username:password@lanplus(192.168.0.118)",
	"username:password@lanplus(192.168.0.119)",
	"username:password@lanplus(192.168.0.120)",
	"username:password@lanplus(192.168.0.190)",
	"username:password@lanplus(192.168.0.189)",
	"username:password@lanplus(192.168.0.187)",
	"username:password@lanplus(192.168.0.191)",
	"username:password@lanplus(192.168.0.192)",
	"username:password@lanplus(192.168.0.193)",
	"username:password@lanplus(192.168.0.194)",
	"username:password@lanplus(192.168.0.99)",	
	"username:password@lanplus(192.168.0.199)",
	"username:password@lanplus(192.168.0.26)",
	"username:password@lanplus(192.168.0.27)",
	"username:password@lanplus(192.168.0.28)",
	"username:password@lanplus(192.168.0.29)",
	"username:password@lanplus(192.168.0.31)",
	"username:password@lanplus(192.168.0.32)",
	"username:password@lanplus(192.168.0.33)",
	"username:password@lanplus(192.168.0.34)",
	"username:password@lanplus(192.168.0.35)",
	"username:password@lanplus(192.168.0.36)",
	"username:password@lanplus(192.168.0.12)",
	"username:password@lanplus(192.168.0.14)",
	"username:password@lanplus(192.168.0.15)",
	"username:password@lanplus(192.168.0.16)",
	"username:password@lanplus(192.168.0.211)",
	"username:password@lanplus(192.168.0.212)",
	"username:password@lanplus(192.168.0.213)",
	"username:password@lanplus(192.168.0.214)",
	"username:password@lanplus(192.168.0.215)",
	"username:password@lanplus(192.168.0.11)"
	]
	privilege = "ADMINISTRATOR"
	#driver = "LAN_2_0"
	timeout = "20s"
	interval = "60s"
	metric_version = 2

[[inputs.exec]]
  commands = ["/etc/telegraf/telegraf.d/scripts/storage.sh"]
  timeout = "120s"
  data_format = "influx"
  interval = "60s"